# Velocity Engine - Python Dependencies
# Install: pip install -r requirements.txt

# Broker SDKs
alpaca-py==0.28.1          # Official Alpaca SDK (paper + live trading)

# Data & Indicators
pandas==2.2.2              # DataFrames for OHLCV data
pandas-ta==0.4.71b0        # Technical indicators (BB, RSI, ATR, ADX, SMA)
numpy==1.26.4              # Numerical computing

# Database
sqlalchemy==2.0.30         # ORM + async support
asyncpg==0.29.0            # Async PostgreSQL driver
psycopg2-binary==2.9.9     # Sync PostgreSQL driver (for migrations)
alembic==1.13.1            # Database migrations

# Scheduling
apscheduler==3.10.4        # Cron-like job scheduler

# API Server (optional dashboard backend)
fastapi==0.111.0           # Async REST API
uvicorn==0.30.1            # ASGI server
websockets==12.0           # WebSocket support

# VIX Data
yfinance==0.2.38           # Yahoo Finance (VIX feed)

# Utilities
python-dotenv==1.0.1       # .env file loading
structlog==24.2.0          # Structured JSON logging
httpx==0.27.0              # Async HTTP client
pytz==2024.1               # Timezone handling

# Testing
pytest==8.2.1
pytest-asyncio==0.23.7
pytest-cov==5.0.0

# Type checking (optional)
mypy==1.10.0
